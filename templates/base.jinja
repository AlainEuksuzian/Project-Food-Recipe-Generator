<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Food Recipe</title>
</head>
<body>
    <div class="topnav">
  <a class="active" href="{{url_for('home_page')}}">Home</a>
  <a href="{{url_for('sign_up')}}" class="login">signup</a>
  <a href="{{url_for('recipe')}}" class="singup">recipe</a>
  <a href="{{url_for('login')}}" class="loginBtn">login </a>
</div>
{% block login %}{% endblock login %}
{% block signup %}{% endblock signup %}
{% block homepage %}{% endblock homepage %}
{% block formTester %}{% endblock formTester %}
{% block recipe %}{% endblock recipe %}

</body>
<style>


.login{
    position: relative;
    left: 80%;
    

}

/* Add a black background color to the top navigation */
.topnav {
  background-color: #333;
  overflow: hidden;
}

/* Style the links inside the navigation bar */
.topnav a {
  float: left;
  color: #f2f2f2;
  text-align: center;
  padding: 14px 16px;
  text-decoration: none;
  font-size: 17px;
}

/* Change the color of links on hover */
.topnav a:hover {
  background-color: #ddd;
  color: black;
}

/* Add a color to the active/current link */
.topnav a.active {
  background-color: #04AA6D;
  color: white;
}
</style>
</html>